## Get directory of JAR
	String path = TestManager.class.getProtectionDomain().getCodeSource().getLocation().getPath();
	System.out.println(URLDecoder.decode(path, "UTF-8").substring(0, path.lastIndexOf("/") + 1));
